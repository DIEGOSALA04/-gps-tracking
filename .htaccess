# Configuración para Apache (cPanel)
# Este archivo debe ir en la raíz de tu aplicación

# Habilitar mod_rewrite
RewriteEngine On

# Habilitar proxy (necesario para redirigir a Flask)
RewriteCond %{HTTP_HOST} ^(www\.)?gps\.megamusic\.com\.co$ [NC]
RewriteRule ^(.*)$ http://127.0.0.1:5000/$1 [P,L]

# O si lo anterior no funciona, usar redirección simple:
# RewriteRule ^(.*)$ http://127.0.0.1:5000/$1 [P,L]

# Permitir acceso a archivos estáticos directamente
<FilesMatch "\.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|json)$">
    RewriteEngine Off
</FilesMatch>

# Deshabilitar listado de directorios
Options -Indexes

# Configurar página de inicio (si Flask no está corriendo)
DirectoryIndex index.html app.py

